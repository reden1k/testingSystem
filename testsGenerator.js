import readlineSync from 'readline-sync'; 

export const makeTest = () => {
    const input = readlineSync.question('How much question do you want? (limit 40)'); //maybe change limit
    //some logic
}